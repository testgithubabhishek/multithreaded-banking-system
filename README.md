# Multithreaded Database-Driven Banking System

A Java backend application simulating a concurrent, multi-user banking environment.

## Current Stage
* **Phase 1 (Initial Setup):** Storing dynamic objects using an ArrayList baseline.

## Future Milestones
* Interactive switch-case console UI loop.
* Database persistence with MySQL and JDBC.
* Multithreading via ExecutorService for concurrent transactions.
